import React from "react";
import { Text, View } from "react-native";

const DrawerScreenOne = () => {
  return (
    <View
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>This is drawer screen one.</Text>
    </View>
  );
};

export default DrawerScreenOne;
